import { Typography } from "@mui/material";
import { FC } from "react";

const LoadingPlaceholderView: FC = () => {
  return <Typography>Loading...</Typography>;
};

export default LoadingPlaceholderView;
